# 启动项目

```shell
pnpm dev
```

# 忘记管理员密码

1. 在运行小松鼠的终端窗口按 `Control+C`，先停止服务。
2. 执行 `pnpm auth:reset-admin`。
3. 按提示输入两次新的 `admin` 密码，输入内容不会显示在屏幕上。
4. 执行 `pnpm dev` 重新启动。

重置管理员密码不会删除职位、简历、人才库或面试数据。普通用户忘记密码时，由管理员在小松鼠的“账号管理”中修改即可。

# 提交代码提示词

帮我把项目git commit一下，然后push到远程

# 拉取远程最新代码提示词

帮我把项目git pull一下，然后merge到本地

# 手动打开文件存储

command+空格 搜索docker
